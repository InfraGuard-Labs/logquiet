/logquiet.exe
/logquiet
/fixturegen.exe
/fixturegen
/diag.exe
/dist/
/dist-goreleaser/
/dist-v0.1.1/
*.test
*.prof
.DS_Store

# Large generated benchmark corpora - reproducible via:
#   go run ./tools/fixturegen perf <lines> <profile> <outfile>
# Correctness fixtures (fixtures/synthetic/*.log) are small and ARE
# committed; only the large raw-throughput corpora are excluded.
/benchmarks/data/
/benchmarks/results/run.log
